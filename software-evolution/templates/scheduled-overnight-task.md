# Scheduled Overnight Task Prompt

Use `$software-evolution overnight` on the configured local project.

Work autonomously within `.software-evolution.yml` and repository rules. If the governance control plane is missing, initialize it automatically and continue in the same run. Use an isolated worktree when available. Repeatedly discover, prove, prioritize, repair, test, verify, re-scan, update engineering memory, and checkpoint safe R1/R2 batches until the overnight budget or a hard stop condition is reached.

Do not wait for routine confirmation. Record decision-, specialist-, environment-, or approval-blocked items and continue independent safe work. Never deploy, mutate production/shared data, change permissions/credentials/alerts/flags, publish remotely, rewrite Git history, or perform R3/R4 contract/model changes without separate exact authorization.

Reserve the configured final verification window. End with the `RUN-*` ledger, exact tests/results, changed files, remaining risks, stop reason, and `$software-evolution resume <id>` when work remains.
